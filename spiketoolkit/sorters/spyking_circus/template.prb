total_nb_channels = $num_channels$
radius            = $radius$
channel_groups    = {1 : {}}

channel_groups[1]["channels"] = range(0, $num_channels$)
channel_groups[1]["graph"]    = []
channel_groups[1]["geometry"] = $geometry$